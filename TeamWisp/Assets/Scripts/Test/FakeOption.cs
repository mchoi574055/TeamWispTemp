using UnityEngine;

namespace Test
{
    public class FakeOption : MonoBehaviour
    {
        private int someValue;
    
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }

        public void SetValue(int something)
        {
            someValue = something;
        }
    }
}
